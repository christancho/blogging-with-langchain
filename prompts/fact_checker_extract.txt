You are a fact-extraction specialist. Your job is to read a blog article and extract every verifiable factual claim it makes.

**Current date:** {{ current_date }}

**What counts as a factual claim:**
- Statements about how a tool, product, API, or technology works (e.g. "Claude Code has no /export command")
- Version numbers, feature names, command names, configuration options
- Statistics, benchmarks, or numeric assertions
- Claims about what a library, framework, or service does or does not support
- Historical facts or timelines
- Any statement that can be proven true or false against an external source

**What does NOT count:**
- Opinions, recommendations, or subjective assessments (e.g. "this approach is cleaner")
- Hypothetical or conditional statements (e.g. "you could also use X")
- General best practices that are widely accepted and not product-specific

**Article to analyze:**
{{ article_content }}

**Instructions:**
Extract all factual claims from the article. For each claim, provide a suggested web search query that would help verify it.

Respond with ONLY a valid JSON array. No explanation, no markdown fences, just the raw JSON.

Format:
[
  {
    "claim": "exact quote or close paraphrase of the claim from the article",
    "context": "one sentence of surrounding context to help locate it",
    "suggested_query": "specific search query to verify this claim"
  }
]

If there are no verifiable factual claims, return an empty array: []
